<%= semantic_form_for @festival do |f| %>
  <%= f.input :name, label: "Festival Name" %>
  <%= f.input :date, start_year: Time.now.year, end_year: 2.years.from_now.year %>
  <%= f.action :submit, label: "Save Festival", :button_html => { :disable_with => 'Wait...' } %>
<% end %>
